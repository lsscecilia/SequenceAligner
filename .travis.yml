dist: trusty
sudo: required
language: cpp
compiler: gcc

addons:
	apt:
		sources:
			-ubuntu-toolchain-r-test
		packages:
			-gcc-9
			-g++-9
			-cmake

script:	
    - mkdir build
    - cmake --build build -- -j3
    - ./bin/mapper_test
