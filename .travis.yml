dist: trusty
sudo: required
language: cpp
compiler: gcc

addons:
	apt:
		sources:
			-ubuntu-toolchain-r-test
		packages:
			-gcc-9
			-g++-9
			-cmake

script:	
    - mkdir build
    - cd build
    - cmake -DCMAKE_BUILD_TYPE=Release ..
    - make
    - ./bin/pink_mapper_test
